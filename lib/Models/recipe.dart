class Recipe {
  final int id, minutes;
  final String name, nutrition, steps;

  Recipe({
    required this.id,
    required this.name,
    required this.minutes,
    required this.nutrition,
    required this.steps,
  });
}
